# Core Python dependencies for the Conditional-GQE suite
pyyaml>=6.0
numpy>=2.0
scipy>=1.14
torch>=2.0
matplotlib>=3.9
tqdm>=4.66
openfermion>=1.7
openfermionpyscf>=0.5
pyscf>=2.13
qiskit>=2.0
qiskit-algorithms>=0.4
qiskit-nature>=0.7

# Model checkpoint downloads from Hugging Face
huggingface_hub>=0.24

# PDF generation
fpdf>=2.8

# QPU access via qBraid
qbraid-sdk>=0.9

# RL training (QD-GRPO + MAP-Elites)
# For NVFP4 on B200/Blackwell, install separately:
#   pip install --no-build-isolation transformer_engine[pytorch]
# CUDA-Q is pre-installed on qBraid; for local install:
#   pip install cudaq
